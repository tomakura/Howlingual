# 更新内容まとめ (2026-02-01)

## 目的
- 非ストリーム時の「Gen」表示を抑制。
- JSONモード非対応プロバイダでもストリーミングで動かせるようにする。

## 変更点
- `src/lib/translation_policy.ts`
  - JSONモード非対応でもストリーミングを許可（`jsonMode: false`で実行）。
- `src/lib/ai_service.ts`
  - OpenAI互換ストリーミングで`jsonMode`を切替可能に。
  - ストリーミング時にJSONモード無効でもパースできるよう既存パーサを活用。
- `src/routes/service/+page.svelte`
  - 実行計画から`streamMode`を算出し、techMetricsに追加。
- `src/routes/+page.svelte`
  - 非ストリーム時は「Gen」表示を省略。
  - techMetricsの初期化/リセットに`streamMode`を追加。

## 影響範囲
- Cerebras/Anthropic などJSONモード非対応プロバイダの翻訳実行
- 技術メトリクス表示（Wait/Gen/Total）

## 確認ポイント
1. Cerebras Qwen3 235Bでストリーミング表示できるか
2. 非ストリームモデルでは「Gen」が表示されないか
3. ストリーム時は従来どおり「Gen」が表示されるか
